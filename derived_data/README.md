The `derived_data/` tree contains the bundled assets used by the public Route A rebuild.

The bundle includes exact archived extractions where available and curated Route A support summaries where full upstream tables are not redistributed.

See `docs/panel_support_asset_manifest.tsv` and `docs/data_provenance.md` for asset-level provenance and fidelity classes.
